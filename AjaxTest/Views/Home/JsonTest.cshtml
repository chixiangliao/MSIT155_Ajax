@{
    ViewData["Title"] = "JsonTest";
}

<h1>JsonTest</h1>

<ul id="test">

</ul>

@section Scripts {
    <script>

        const test = document.getElementById('test');
        const emps = [{ "name": "Tom", "workYears": 3, "salary": 35000 },
        { "name": "Jack", "workYears": 5, "salary": 40000 },
        { "name": "Mary", "workYears": 7, "salary": 45000 }];

        var emp = {
            "employees": [
                { "name": "Tom", "workYears": 3, "salary": 35000 },
                { "name": "Jack", "workYears": 5, "salary": 40000 },
                { "name": "Mary", "workYears": 7, "salary": 45000 }
            ]
        };

        

        let mm = "";

        // for (let i = 0; i < emps.length; i++) {
        //     mm += `<li> ${emps[i].name}, ${emps[i].workYears}, ${emps[i].salary}</li>`
        // };
        // test.innerHTML = mm;

        // emps.forEach(emp => {
        //     mm += `<li> ${emp.name}, ${emp.workYears}, ${emp.salary}</li>`
        // })
        // test.innerHTML = mm;

        const liELe = emps.map(emp => `<li> ${emp.name}, ${emp.workYears}, ${emp.salary}</li>`).join("");
        console.log(liELe);
        test.innerHTML = liELe;
    </script>
}

